---
description: "Scaffold a bundle-plugin or manage platform support — use the bundles-forge:scaffolding skill instead"
---

This command invokes the scaffolding skill. Use it for generating new projects, adding or removing platform support, or updating platform manifests and hooks.

Invoke the `bundles-forge:scaffolding` skill to begin.
