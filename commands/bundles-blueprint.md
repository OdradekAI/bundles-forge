---
description: "Plan a new bundle-plugin — use the bundles-forge:blueprinting skill instead"
---

This command invokes the blueprinting skill. Use it to plan a new bundle-plugin through structured interview.

Invoke the `bundles-forge:blueprinting` skill to begin.
