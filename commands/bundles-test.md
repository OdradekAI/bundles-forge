---
description: "Test a bundle-plugin locally — use the bundles-forge:testing skill instead"
---

This command invokes the testing skill. Use it to verify a bundle-plugin works correctly through local installation, hook smoke tests, and component discovery.

Invoke the `bundles-forge:testing` skill to begin.
