{
  "description": "Bundles Forge bootstrap: injects skill routing context on session start",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd\" session-start",
            "timeout": 10,
            "async": false
          }
        ]
      }
    ]
  }
}
